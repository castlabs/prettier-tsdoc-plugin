{
  "name": "@castlabs/prettier-tsdoc-plugin",
  "version": "0.5.7",
  "description": "A prettier plugin to handle TSDoc comments",
  "license": "MIT",
  "author": "castLabs",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "rollup -c",
    "typecheck": "tsc --noEmit",
    "prettier": "prettier --log-level warn --write .",
    "test": "vitest --run",
    "test:silent": "vitest --run --silent",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "check": "npm run prettier && npm run lint:fix && npm run typecheck && npm run build && npm run test:silent",
    "example:prettier": "cd example && npm run prettier",
    "example:typedoc": "cd example && npm run typedoc",
    "example:tsc": "cd example && npm run tsc",
    "example:api": "cd example && npm run api",
    "example:check": "npm run build && npm run example:prettier && npm run example:tsc && npm run example:api && npm run example:typedoc"
  },
  "dependencies": {
    "@microsoft/tsdoc": "^0.15.1",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "prettier": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-typescript": "^12.1.4",
    "@types/node": "^20.0.0",
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-tsdoc": "^0.4.0",
    "rollup": "^4.46.1",
    "tslib": "^2.8.1",
    "typescript-eslint": "^8.38.0",
    "vitest": "^3.2.4"
  }
}
